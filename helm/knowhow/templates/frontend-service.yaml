{{- if .Values.frontend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "knowhow.fullname" . }}-frontend
  labels:
    {{- include "knowhow.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "knowhow.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
{{- end }}
