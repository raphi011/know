apiVersion: v1
kind: Service
metadata:
  name: {{ include "know.fullname" . }}
  labels:
    {{- include "know.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "know.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
